import React from 'react';
import ReactDOM from 'react-dom';
import Contenido from './Components/Contenido';
import './sass/app.scss';

const divRoot = document.getElementById('root');

ReactDOM.render(<Contenido />, divRoot);

